import React from "react";
import Contacts from "../components/Contacts";
import Layout from "../components/Layout";

const List = () => {
  return (
    <Layout>
      <Contacts />
    </Layout>
  );
};

export default List;
